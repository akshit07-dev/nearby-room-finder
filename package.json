{
  "name": "nearby-room-finder-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n \"API,VITE\" -c \"blue,green\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "server": "npm --prefix backend run dev",
    "client": "npm --prefix frontend run dev",
    "seed": "npm --prefix backend run seed",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "lucide-react": "^1.34.0",
    "mongoose": "^9.9.4",
    "moongose": "^1.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
